if (get_application_name()=="Disc Burner") then
        undecorate_window()
if (get_window_is_maximized() == false) then
        unminimize()
end
	set_window_geometry2(2,186,1366,528);
        make_always_on_top()
        stick_window()
end

